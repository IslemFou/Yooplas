{% extends 'base.html.twig' %}

{% block body %}
    <div class="container my-5">
        {# <h1>Mes évenements</h1> #}
        
        <h6>{{ event.title }}</h6>
        <p> {{ event.events|length }} évenements </p>
    
        {# </header> #}
        {# <main class="container min-vh-100"> #}
            {# <div class="row mt-4"> #}
            {{dump(event)}}
           

                    {# card #}
                    {# <div class="card col-sm-12 col-md-4 col-lg-3 rounded-4 shadow m-2 mb-5 p-0" style="height:37rem;">

					<img src="/uploads/events/{{ event.photo}}" class="card-img-top rounded-top-4 img-fluid" style="height:25rem; width:100%; object-fit: cover;" alt={{ event.title}}">
					<div class="card-body">
						<div class="mx-2 d-flex justify-content-between">
							<p class=" small fs-6 mb-0"><i class="fbi bi-geo"></i>{{ event.city}}</p>
							<span class="badge small mb-3 text-yoopla-blue rounded-pill p-2 fw-medium border"><a href="{{ path('app_category', { slug : event.category.slug})}}" class="text-decoration-none text-yoopla-blue">{{ event.category.name }}</a></span>
						</div>
						<h5 class="fs-5 card-title">{{ event.title}}</h5>
						<p class="small card-text text-muted">{{ event.description|raw }}</p>
					</div>
					<div class="d-flex justify-content-center">
						<a href="" class="btn yoopla-primary fw-medium rounded-5 px-4 py-2 shadow mb-3">Voir l'activité</a>
					</div>
				</div> #}
                    {# card #}
                
                
                {# </div> #}


                {# ------- #}
                {% if event %}

<section class="container position-relative">
    <div class="container d-flex justify-content-around m-3">
        <h1 class="text-center p-2 fw-bold m-2">{{ event.title ?? 'Titre non disponible' }}</h1>
        <p class="text-center fw-bold m-2 p-2 text-yoopla-blue tracking-wider lh-lg fs-4">{{ event.price ?? 'prix non disponible' }}€</p>
    </div>
    <div class="container">
        <img src="/uploads/events/{{ event.photo ?? 'photo non disponible' }}" class="card-img-top rounded-4" style="height:25rem; object-fit: cover;" alt="image evenement" title="image evenement">
    </div>
    <div>
        {# <h5 class="fw-medium m-2">Organisateur</h5>
        <div class="d-flex align-items-center justify-content-start">
            <img src="<?= BASE_URL . '/assets/images/default-img/default_avatar.jpg'; ?>" class="rounded-circle" width="50" height="50" alt="image de profil par défaut" title="Organisateur de l'evenement">
            <p class="fw-bold m-3"><?= htmlspecialchars(($event['firstName'] ?? '') . ' ' . ($event['lastName'] ?? ''), ENT_QUOTES, 'UTF-8'); ?></p>
        </div> #}
        <div class="d-flex align-items-center flex-wrap justify-content-evenly">
            <p><i class="bi bi-geo"></i>{{ event.city ?? 'information non disponible' }}</p>
            <div class="d-flex justify-content-between align-items-center">
                 <p class="text-light-dark"><i class="bi bi-clock"></i>{{ event.timeStart|date('H:i') ?? ?? 'information non disponible'}}</p>
                <p>
                <p class="text-light-dark ms-3"><i class="bi bi-calendar-event mx-2"></i>{{ event.dateStart|date('d/m/Y') ?? 'information non disponible' }}</p>
            </div>
            <p><i class="bi bi-people"></i>{{event.capacity ?? 'information non disponible'}} personnes</p>
            <div class="fs-6 btn rounded-5 px-4 fw-medium shadow-sm yoopla-secondary mb-3"><a href="{{ path('app_category', { slug : event.category.slug})}}" class="text-decoration-none text-yoopla-blue">{{ event.category.name }}</a></div>
        </div>
        <!-- avatar -->
        <div>
            <h5 class="fw-medium m-2">Participants</h5>
            <div class="avatar-stack mx-4">
                <span class="avatar bg-discovery z-index-1">+6</span>
                <img class="avatar" style="height:3rem; width:3rem" src="{{ asset('assets/images/default-img/person1.png')}}" style="height:3rem; width:3rem" title="participant 1" alt="image_avatar" />
                <img class="avatar" style="height:3rem; width:3rem" src="{{ asset('assets/images/default-img/person2.png')}}" style="height:3rem; width:3rem" title="participant 2" alt="image_avatar" />
                <img class="avatar" style="height:3rem; width:3rem" src="{{ asset('assets/images/default-img/person3.png')}}" style="height:3rem; width:3rem" title="participant 3" alt="image_avatar" />
                <img class="avatar" style="height:3rem; width:3rem" src="{{ asset('assets/images/default-img/person1.png')}}" title="participant 4" alt="image_avatar" />
            </div>
        </div>
        <div class="m-3 container bg-discovery-subtle rounded-4 p-3">
            <h6>Description</h6>
            <p>{{ event.description|raw ?? 'information non disponible' }}</p>
        </div>
        <!-- Bouton de réservation -->
        <div class="d-flex justify-content-center">

            {# ----------------------- Debut modal ---------------------------------------------  #}
            <!-- Button trigger modal -->
            {# <?php
            if (isset($_SESSION['user']) && $_SESSION['user']['ID_User'] != $id_user_event) {
            ?> #}

                <!-- Bouton de reservation -->
                <div class="btn btn-yoopla-primary fw-medium rounded-5 px-4 py-2 shadow m-3" data-bs-toggle="modal" data-bs-target="#exampleModal">Reserver</div>

            {# <?php } elseif (isset($_SESSION['user']) && $_SESSION['user']['ID_User'] == $id_user_event) { ?> #}
                <!-- Bouton de suppression -->
                <div><a href=" <?= BASE_URL ?>event/showEvent.php?action=delete&ID_Event=<?= $event['ID_Event'] ?>" class="btn btn-yoopla-secondary-outlined fw-medium rounded-5 px-4 py-2 shadow m-3 text-decoration-none text-light">Supprimer</a></div>
                <!-- Bouton de modification -->
                <div><a href=" <?= BASE_URL ?>event/addEvent.php?action=update&ID_Event=<?= $event['ID_Event'] ?>" class="btn btn-yoopla-primary fw-medium rounded-5 px-4 py-2 shadow m-3 text-decoration-none text-light">Modifier l'evenement </a></div>


            {# <?php } else { ?> #}

                <div><a href=" login.php" class="btn btn-yoopla-primary fw-medium rounded-5 px-4 py-2 shadow m-3 text-decoration-none text-light">Se connecter</a></div>
            {# <?php } ?> #}


            {# -- Modal: affichage de la modale -------------------------------------------------------------- #}

            <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h1 class="modal-title fs-5 mx-auto text-center" id="exampleModalLabel">Terminer la réservation</h1>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                        </div>
                        <div class="modal-body">
                            <p class="text-regular mx-auto text-center"><i class="bi bi-envelope-check text-yoopla-red fs-5"></i> Votre adresse e-mail sera partagé avec l’organisateur de l’évènement.</p>

                            {# ------------ Envoi de la réservation ( dans le cadre d'une demande de reservation ) --------------------------------------------------------------- #}

                            <form action="#" method="POST">
                                <input type="text" name="ID_Event" value="<?= $event['ID_Event'] ?>" hidden>
                                <input type="text" name="ID_User" value="<?= $_SESSION['user']['ID_User'] ?>" hidden>
                                <input type="text" name="status" value="accepted" hidden>
                                <label for="message" class="form-label mb-3">Ajouter un message de reservation</label>
                                <textarea name="message" id="message" cols="30" rows="3" placeholder="Une petite remarque ?" class="form-control"></textarea>
                                <div class="modal-footer d-flex justify-content-center">
                                    <button type="submit" name="submit" class="mx-center btn btn-yoopla-primary fw-regular rounded-5 px-5 py-2 shadow m-3 mx-auto">Confirmer</button>
                                </div>
                            </form>

                        </div>
                    </div>
                </div>
            </div>

            {# <----------------------- fin modal --------------------------------------------------- #}


        </div>
    </div>
</section>

        </div>
    </div>
{# ----------------------- Fin modal ---------------------------------------------  #}




{# Au niveau de cette partie, on cache la bande de reservation si l'utilisateur connecté est l'organisateur de l'evenement lorsque l'utilisateur connecté est lui meme l'organisateur de l'evenement.

De ce fait, il n'y a pas de bouton de reservation et la bande de reservation est cachee. #}

{# if (isset($_POST['ID_Event'], $_POST['ID_User'])) {

    $id_user = (int)$_SESSION['user']['ID_User'];

    if ($id_user  == $event['ID_User']) {
        $hiddenClass = 'd-none'; #}


        <!-- début Bande de réservation -->
        <div class="d-flex align-items-center justify-content-between border border-yoopla-primary rounded-top-4 p-3 position-sticky shadow bottom-0 bg-white <?= $hiddenClass ?>">
            <div>
                <div class="d-flex justify-content-between align-items-center">
                    <p class="text-light-dark"><i class="bi bi-clock"></i>{{ event.timeStart|date('H:i')}}</p>
                    <p>
                    <p class="text-light-dark"><i class="bi bi-calendar-event mx-2"></i>{{ event.dateStart|date('d/m/Y') }}</p>
                </div>
                <h5>{{ event.title }}</h5>
            </div>
            <form action="" method="POST" class="d-flex justify-content-center">
                <p class="text-center fw-bold m-3 p-2 text-yoopla-blue tracking-wider lh-lg">{{ event.price }}€</p>
                <button class="btn btn-yoopla-secondary-outlined fw-medium rounded-5 px-4 py-2 shadow m-3">Enregistrer<i class="m-1 bi bi-heart"></i></button>
                <!-- Bouton de réservation -->

                <!-- modal -->
                <!-- Button trigger modal -->
                <button type="button" class="btn btn-yoopla-primary fw-medium rounded-5 px-4 py-2 shadow m-3" data-bs-toggle="modal" data-bs-target="#exampleModal">
                    Réserver
                </button>
                <!-- Modal: affichage de la modale -->
            </form>
        </div>

                                
                                
{% else %}

<p>Aucun événement trouvé.</p>

{% endif %}               
{% endblock %}
